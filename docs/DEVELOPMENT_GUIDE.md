# Development Guide

Inspect the existing code and applicable current documentation before making changes. Extend the existing ViewModel and command pattern, preserving existing behavior unless the task explicitly changes it.

Implement the smallest coherent change. Avoid unrelated cleanup, new packages, and speculative abstractions. Keep validation proportionate to the affected behavior.
