# Decisions

- Binary state remains a `bool`.
- The initial state is `false`.
- Exact status text values are `0` and `1`.
- `false` is red and `true` is green.
- State belongs in the ViewModel.
- Commands use `ICommand`.
- Persistence is not authorized.
- A third-party MVVM framework is not justified.
