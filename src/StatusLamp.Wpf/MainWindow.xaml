<Window
  x:Class="StatusLamp.Wpf.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="StatusLamp"
  Width="320"
  Height="300"
  ResizeMode="NoResize"
  WindowStartupLocation="CenterScreen"
>
  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Margin="0,0,0,24" Background="{Binding StatusColor}" CornerRadius="12">
      <TextBlock
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        FontSize="72"
        FontWeight="SemiBold"
        Foreground="White"
        Text="{Binding StatusText}"
      />
    </Border>

    <Button Grid.Row="1" Height="40" Command="{Binding ToggleCommand}" Content="Toggle" />
  </Grid>
</Window>
